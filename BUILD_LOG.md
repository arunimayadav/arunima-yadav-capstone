# Build Log

**Running total (Assessments 1–3):** ~24 hrs · ~493k tokens

## 2026-09-11
- **Time spent:** ~1 hr
- **Tokens used:** ~3,000 (plan.md drafting via Claude in plan mode)
- **Shipped:** Capstone idea locked in, repo created, `project-setup` branch, plan.md committed with descriptive message

## 2026-09-17
- **Time spent:** ~3 hrs total (most of it went into figuring out API key formatting, MCP server setup, and .gitignore; the actual agent build (once things were connected) took Claude Code about 7 minutes)
- **Tokens used:** ~40k
- **Shipped:** Finalized and committed the filename-nomenclature Skill (filename-nomenclature.md) with the help of Claude. Set up .env for the Gemini API key and added .gitignore to keep it out of the repo. Connected a filesystem MCP server. Had Claude Code build the full agent (perceive → reason → act → observe) using the Skill for naming. First run failed: invalid API key turned out to be a stray space after the `=` in .env, plus the key needed header auth instead of a URL param, and the model name was deprecated so it got repointed to a current one. After fixing those, successfully ran the agent end-to-end on a real downloaded file- read it, classified it via Gemini, renamed it per the Skill's rules, and moved it into the correct Archive subfolder via the MCP server. Then worked on renaming a newly downloaded file.

## 2026-09-18
- **Time spent:** ~4 hrs (plan revisions, scaffolding the new Swift app, then losing time to a self-inflicted git incident)
- **Tokens used:** ~90k
- **Shipped:** Pivoted the plan away from the Python/CLI agent toward a native macOS Swift menu bar app, and rewrote plan.md around graph memory, an always-on FSEvents watcher, and a backfill scan for files that predate the app. Had Claude Code scaffold the initial macapp Swift package with Search, Organize, Review, and Settings tabs. Mid-session, a commit meant to capture the redesign (`Redesign for graph memory, automatic watcher, and backfill scan`) accidentally swept in a `.venv` and a `.env` file containing a live API key. Caught it, added a proper `.gitignore` to stop it recurring, and had to delete and recover a branch to get the key out of tracked history — lost a chunk of the session to cleanup rather than feature work, and some of the just-built macapp scaffolding had to be reconstructed afterward since it hadn't been safely committed yet.

## 2026-09-19
- **Time spent:** ~6 hrs (steady stream of small fixes from early afternoon into the evening, plus a final hour closing out remaining naming edge cases)
- **Tokens used:** ~120k
- **Shipped:** Fixed a startup crash in FileWatcher.swift — the FSEvents callback was treating `eventPaths` as a raw C array of pointers when `kFSEventStreamCreateFlagUseCFTypes` actually hands back a CFArray, so indexing into it read garbage memory and crashed almost immediately; switched to `CFArrayGetValueAtIndex`. Also fixed the watcher picking up every pre-existing file in Downloads on launch instead of only genuinely new files after start. Added the tagging Skill (skills/tagging.md) and wired it into the processing pipeline alongside filename-nomenclature, then spent several commits (`Fixing rename and tagging not executing after file detection`, `naming fix`, `renaming issue fixing`, `Fixing tagging execution`) chasing renames and tags not firing after file detection, incorrect filenames, and inconsistent tags being applied across similar files — root-caused to gaps in GraphStore, Pipeline, TagWriter, and OllamaProvider not passing existing tags/context through consistently. Wired Search to check filename, content, category, and tags together instead of filename-only matching. Set up Ollama locally as the default free/local AI provider (with cloud providers as opt-in overrides in Settings). Wrote the project README covering setup, requirements, and how the AI provider fallback works. Opened and merged the assessment-2 branch into main via PR #2. **Last hour (`Fixing naming issues`):** handled files getting deleted or moved away out-of-band while still mid-pipeline — added `GraphStore.deleteNode` to clean up a node's tags/edges/moves/row, and had Pipeline check for the source file's existence both before attempting a rename and after a move failure, so a vanished file leaves behind a clean deletion instead of a permanently stuck, unrenamed graph entry. Added one automatic retry to `ProviderRouter`'s Ollama call before falling back, since local Ollama response times were swinging from ~30s to 150s+ on the same kind of prompt and a single slow attempt was wrongly being treated as a hard failure. Changed Search to return only the single closest match instead of a ranked list (relying on "Show related" to surface the rest of the graph).

## 2026-09-22 — Part A
- **Time spent:** ~1 hr
- **Tokens used:** ~20k
- **Shipped:** Filled out the remaining Skills the pipeline in plan.md was already assuming existed. Wrote `content-summarisation.md` (content-derived plain-language summary, independent of category, feeding the node's `summary` field). Wrote `relationship-builder.md` (turns a newly enriched node's tags/category/embedding into `same_tag`/`same_category`/`similar_content` edges against existing nodes, keyed uniquely on source/target/edge_type per the Data Model). Wrote `metadata-enrichment.md` (assembles every upstream skill's output plus timestamps and confidence-derived status into the single node record returned on search). While cross-checking those against each other, noticed every one of them referenced "Classification" as an upstream input but no such Skill file actually existed yet, wrote `classification.md` to close that gap (ownership/category/doc_type/confidence, reuse-over-sprawl on category same as Tagging). Also rewrote `filename-nomenclature.md` entirely around three explicit cases instead of the old two-branch ownership logic: images now name as `<WhatItIs>_<dd-mm-yyyy>`, non-owned files as `<Category>_<Title>`, and owned files as `<Category>_<Title>_<PersonName>`, with is_image checked before ownership so an owned photo still gets the image pattern, not the owned-file one.

## 2026-09-22 — Part B
- **Time spent:** ~2 hrs
- **Tokens used:** ~60k
- **Shipped:** Wrote `skills/review.md` (Accept/Edit/Reject manual-resolution path for pending_review files), then discovered the macapp's `ReviewView.swift` was still display-only — it listed low-confidence files with a "Reveal in Finder" button, but had no Accept/Edit/Reject actions and no code path for them at all. Built the missing half: added `ownership`/`docType`/`title`/`reasoning` to the `Node` model and a `rejected` case to `NodeStatus` (with a `GraphStore` schema migration so an existing `graph.sqlite3` picks up the new columns without a fresh database); added `GraphStore.updateNode`/`setStatus`; pulled the rename+Finder-tag+move-log logic out of `Pipeline` into a shared `Core/FileAction.swift` so the automatic high-confidence path and Review's Accept/Edit can't drift apart; added `Core/ReviewActions.swift` (accept/edit/reject, with Relationship Builder re-run on accept/edit only, per the skill). Rebuilt `ReviewView.swift`'s UI: each pending card now shows ownership/doc type/confidence/the model's one-line reasoning, with real Accept, Edit (inline category/doc-type/title/tags fields), and Reject buttons instead of a read-only list. Added a proper `ArchivistTests` target (there wasn't one) with 7 tests covering the whole review flow end-to-end against a throwaway temp DB — queue fetch, accept (file actually renamed + tagged on disk), edit (corrections overwrite suggestions and drive the filename), reject (file untouched, drops out of the queue without being deleted), and Relationship Builder re-running after accept. Also updated `FilenameNomenclature.swift` to match the morning's three-case skill rewrite (it had been left on the old two-case logic) — added image detection by extension, the hyphenated-lowercase `<WhatItIs>_<dd-mm-yyyy>` pattern for Case 1, and dropped doc type from the Case 2/3 filename pattern — with two more tests confirming the image and owned-file cases. **Mistake:** ran `rm -f` on the real `graph.sqlite3` while clearing test data to try the Review tab live, without asking first — confirmed with the archive owner afterward that it was only test data, so nothing of value was lost, but should have checked before deleting.

## 2026-09-23
- **Time spent:** ~3 hrs 
- **Tokens used:** ~70k
- **Shipped:** Menu bar icon now carries a small red badge dot whenever files are waiting in Review (`AppEnvironment.pendingReviewCount`, polled every 4s and pushed through Combine into `AppDelegate`, which composites the dot onto the folder glyph rather than leaving it a template image, since a template image would flatten the red to the same monochrome tint as the rest of the icon); clicking the icon while badged jumps straight to the Review tab instead of wherever the popover was last left open. Folded Settings out of its own gear-button popover and into the tab system itself as a fourth `ArchivistTab` case, then rebuilt the tab row as a custom `TopBar` (native `Picker(.segmented)` couldn't be restyled to spec) — a pill-style segmented control for Search/Organize/Review plus a separate circular gear button, with an `ArchivistPalette` of exact hex constants introduced so colors used across cards stay identical instead of drifting via ad hoc opacity. Redesigned the badge itself (8px dot with a 1px white border so it reads against any icon color) and switched `TagPill` from a single tint at varying opacity to explicit (background, foreground) pairs per the design spec. Removed the tappable example chips from Organize's empty state. Added `FileThumbnailView` (`QuickLookThumbnailing`, in-memory `NSCache` keyed by path) so Search and Review cards show an actual Quick-Look-style preview of the file's content instead of a generic type glyph; wired into both `SearchView` and `ReviewView`. Reworked `SearchResultCard`: the pill now shows the file's real assigned tag instead of the broader category, tapping the summary expands/collapses it in place, and "Show related" moved out of the card into its own section below so it reads as a separate action rather than part of the card. **Tagging fix:** root-caused same-category files ending up with inconsistent-but-overlapping tags (e.g. "Essay" vs "Essays") to the model being trusted to stay consistent call-to-call, which real usage showed it doesn't — added `GraphStore.primaryTag(forCategory:)` to look up whichever tag is already established for a category, had `Pipeline` override the model's suggested tag with it before writing anything, and rewrote `TagWriter`'s Finder-tag coloring to key off the actual tag rather than category so same-tag files always share a color; added a new Step 0 to `skills/tagging.md` codifying "same category, same tag, every time" as the skill's primary correctness test. **Refining Organising Skill:** added undo for the Organize/Command flow — `CommandInterpreter.undo(_:)` moves files back to their original locations best-effort (skipping any file the user has since touched rather than failing the whole batch) and removes the destination folder if undoing empties it, backed by a new `GraphStore.markMoveReversed` that flips the move-log row and restores the node's path; `CommandView` now shows a "Moved N files to X in Y" confirmation with an inline Undo button after a move, and an "Undone" confirmation after using it — previously confirming a move gave no feedback at all once it completed.

## 2026-09-24
- **Time spent:** ~4 hrs (steady work from late morning into mid-afternoon: a search/tagging correctness pass, then a bigger tagging-system rework, then two rounds of UI polish)
- **Tokens used:** ~90k
- **Shipped:** Fixed several correctness bugs found by exercising the app for real. Search was scoring on any substring match (a bare "c" would `.contains` into "Finance"), so added a minimum term length, dropped 2-letter terms from matching file content specifically (too noisy over full document bodies), and required a minimum total score before a node surfaces at all instead of any score above zero. `RelationshipBuilder` stopped creating `same_category` edges — category is a coarse administrative bucket (every "Course" file regardless of actual subject), so "Show related" was surfacing files with nothing in common besides both being schoolwork; a same-category grouping that's actually meaningful already shows up as a `same_tag` edge. Extracted a shared `FinderLabelColor` enum (names + index computation) so `TagWriter`'s on-disk color and the UI pill's rendered color can never independently drift. Root-caused the model occasionally grabbing an unrelated existing tag just because it was offered as an option (e.g. a finance document tagged "User Experience"); rewrote the AI prompt to return a single `tag` field instead of a list and added an explicit instruction that being in the existing-tag list isn't itself a reason to pick it, plus a matching rewrite of `skills/tagging.md`. That fix held up better than expected but the underlying open-ended tagging design still felt fragile, so mid-afternoon **switched to a fixed 5-category system** (Finance/Academic/Personal/Health/Extra) — added `FixedCategory` as a closed enum with a `from(_:)` normalizer that falls back to `.extra` for any drift, and a *permanent* per-category Finder-tag-color mapping (`finderLabelIndex`) instead of a hashed one. Category now doubles as the tag — there's no separate open-ended tagging decision left to drift. This ripped through the AI prompt (closed 5-way JSON field instead of free text), every provider's `understand()` signature (dropped `existingTags`, no longer needed since there's nothing left to reuse from), `ReviewActions.edit` (normalizes a reviewer's typed category to one of the five so the DB and the Finder tag can't disagree), and `skills/tagging.md` (rewritten around the closed choice). Then did two rounds of visual polish on the result cards: first pass added Quick-Look-style thumbnails (`QuickLookThumbnailing`, preserving each file's natural aspect ratio instead of force-cropping to a square) with a fixed card shadow and tag-pill sizing; second pass restyled thumbnails as an actual page shape with a folded top-right corner ("dog-ear") instead of a plain clipped rectangle, fixed a sizing bug where non-square thumbnails left a visible plain-color margin inside their frame, fixed the popover jumping from top to center when Organize's content got shorter than the window, and reworked Review's card to show a category *picker* (one of the five fixed values) instead of a free-text field, with a persistent "Accepted/Saved/Rejected" confirmation banner at the view level (a per-card banner would unmount with the card the instant its action resolved). Also built, then simplified, a full node detail view: first added `NodeDetailView` as a separate popover surfacing every field `metadata-enrichment.md` says a node should carry (location, status, confidence, doc type, ownership, timestamps, classifying provider); after reviewing it, cut that down to an inline in-card expansion showing only location and the two timestamps (status/confidence/doc type/ownership aren't useful to a user browsing search results, so they stay computed and stored but not displayed), then split the card's tap targets so tapping the card reveals the file in Finder while tapping the summary text specifically toggles the inline expansion — updated `skills/metadata-enrichment.md`'s new Step 5 to match both times.

